/**
 * Google Analytics — REMOVED 2026-07-11 (together with the cookie banner).
 * No tracker loads anywhere on the site. The export stays as a no-op so
 * the many page entries that call initGoogleAnalytics() keep building.
 * Restore from git history (pre-2026-07-11) if analytics returns.
 */

export function initGoogleAnalytics() {
  /* no-op — analytics removed */
}
